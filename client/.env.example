# Existing agent backend (server/ on port 8000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# forex-agent-system backend (api_server.py on port 8001)
NEXT_PUBLIC_FOREX_URL=http://localhost:8001

# Only needed if you use the browser-side Groq analysis route
# GROQ_API_KEY=
